main()
{
    // mode: off, <ruleset>.
    // second weapon: off (default), on (opponent team), <team>, "karmos".
    // pistol: off, on.
    // nades: off, on.
    // afs: off, on.

	// readyup: off, on.
    // rounds: <max number of rounds>. (Or use scr_sd_roundlimit?)
    // halftime: off, on.
    // overtime: off, <mode>
	// count draws: off, on.

    // strat (freeze): <seconds>.
    // round start delay: <seconds>

    // players alive: off, on.
}
